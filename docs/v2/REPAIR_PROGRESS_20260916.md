# 2026-09-16 修复结果与未完成边界

## 当前结论

本地工作台已恢复，可查看最近完整交易日、比较证券并进入判断/复盘流程；专用账户的批处理登录权已实际修复。**现网部署仍未成功，不能标记项目全部整改完成。**

打开 `http://127.0.0.1:8769/`；服务停止后运行项目根目录 `Start Research.cmd`。这只是本机服务，不是已安装开机自启或正式调度。账户未经核对，真实交易继续关闭。

## 本轮已完成

| 问题 | 实际修改或验证 | 尚不代表什么 |
|---|---|---|
| 专用身份从未启动 | 管理员证据确认缺少批处理登录权；按单项批准只增加该账户 `SeBatchLogonRight`，保留原有授权与空拒绝策略 | 账户和任务仍停用；未证明任务启动或部署成功 |
| 失败诊断难以回传 | 安装器保存简明权限诊断；回滚前记录任务/登录失败证据；探针启动前检查批处理登录权 | 不自动补权、启用账户或续期许可 |
| 本地工作台离线、数据陈旧 | 服务恢复并核对源码一致；市场和研究预测都更新至 2026-09-15 | 不冒充 9 月 16 日实时行情；未替换旧现网页面 |
| 旧模型日期混同市场日期 | 候选区拆分市场/预测日期及覆盖范围，异日预测不进入当前比较 | 非空模型估计不等于选股优势或交易资格 |
| 每次读取遍历全部人工历史 | 新增可重建 SQLite 事件索引；接管判断请求去重、修订查询、最近列表及计划失效查询 | 原始不可变事件仍是权威；索引不替代证据核验 |
| 启动与长历史查询耦合 | 启动时显式检查索引；列表最多 100 条，历史使用游标分页；崩溃后重建并复用原请求回执 | 完整多类型混合负载、全部生产规模场景尚未验收 |
| 收盘页生成故障 | 用整改目录代码生成 9 月 15 日真实页面并通过原有审计：390 概念、268 涨停概念、1,083 映射、覆盖率 100% | 约 14.46 MB 页面不等于已通过全部性能验收；旧 live latest 仍未替换 |

批处理权限成功回执：`reports/repair-20260916/batch-repair-5c956ac1649142e69d78669187cae5de.complete.json`。其中 `changes=1`、`account_enabled=false`、`task_enabled=false`；仅此系统权限变更获得并使用了本轮单独批准。

### 管理员核对误判修复（13:05 回执之后）

管理员核对回执 `retained-check-bca3621cc4a54ad4889dcd8254c1f5e2.json` 实际失败，系统变更为 0。读取本次两份原始权限导出后确认：Windows 把已授权的本地账户导出成 `StockDataResearch`，而核对器只认 SID，产生误报；不是批处理登录权再次缺失。

已用 Windows 原生名称到 SID 解析修正允许/拒绝项比较，解析失败仍关闭执行；不修改权限、账户或任务。50 项部署/恢复专项测试通过，包括新增 6 项名称、SID、拒绝项及错误身份回归；Windows PowerShell 5.1 只读重放两份现场导出均通过，同名拒绝策略仍被阻止。修正版完整管理员 Check 已于 13:20 通过，回执 `retained-check-3b6d2d6adf06418eb8c1b03f6fe70155.json`，系统变更为 0。用户已单独批准一次离线启动/只读权限验证；因原盘中任务仍运行，实际探针尚未启动，须等其自然结束，不停止原任务。详见 [部署修复记录](DEPLOYMENT_REPAIR_20260913.md)。

### 数据更新与两组原队列复盘

- 原有 43 只研究证券，218 个有界请求；复用封存前缀，只请求 2026-09-08 至 2026-09-16 的增量/修订窗口，没有重取完整 90 天。沿用冻结模型，不训练、不替换模型、不写现网主库。
- 最新 2026-09-15 有 40 条非空预测。市场事实覆盖 5,572 只，前后日同证券/来源/复权/单位的广度比较覆盖 5,562 只。两者不是同一分母，也不是模型研究样本数量。
- 本次预测生成于 9 月 16 日午间，已经错过目标开盘，`forecast_status=after_entry`、`prospective_eligible=false`，不计前瞻成绩。模型训练截止仍是 2025-08-28，候选模型没有通过替换门槛。
- 原来冻结于 9 月 11 日的两组队列，现在可用 9 月 14 日进入、9 月 15 日退出的实际价格核对。两组各保留 43 只证券，具备模型配对的分别是 14、40 只，状态均为 `mature`。这是两组价格目标复盘，不是两次独立投资收益实证；不能相加为独立样本或当作真人验收。
- 真人判断仍为 0。未判断项保留，没有代填记录；价格目标成熟也不能自动认定人工失效条件触发。

该次封存结果：`reports/research-delivery/observations/3f5e233436434802aeffbb4973f81c2e/predictions.json`；模型 ID `c903152dfceaffa27abd3f775518561e419922279dd180e9da952f3d21e1acdc`；预测 ID `e1aea7e47bf4004dbe8eac6342fc98c242ad33b24d888519439cd2a3c71a9b2d`。

### QLib 对照结论没有放宽

重新独立核算已有研究输入，677 个样本、40 天，其中 23 天满足选取前 5 只所需横截面。模型相对动量的平均价格目标差约 -0.0009 个百分点，没有证明更优；相对等权约 -2.2511 个百分点。已暴露、经过可用性选择的样本和价格代理目标，不能证明普遍投资效用；这也不是实际组合回报或全新留出检验。`superiority_proven=false`、`promotion_allowed=false` 保持不变。

证据：`reports/repair-20260916/utility/result.json`；该核算没有训练、取数或改变研究输入。

## 代码、负载与发布包验证

本次权限名称解析修正后的源码 SHA-256 为 `8925ac38688640b72d83e122767296cc76298669954e23397ba853a492f3be87`，**1,650 项通过、0 失败/错误/跳过**（14 条警告），回执 `reports/recovery-policy-20260916/verification/verification.json`。lint、迁移、写者边界和已安装核心检查均通过，测试期间源码未变化。此结果不替代管理员 Check、实际身份启动或现网验收。

以下首轮 1,623 项及第二轮 1,644 项记录保留作历史。第二轮源码 SHA-256 为 `4c8b2acc1e6762d9ea50c8d782aca82f8f54bc69452800174390802a5ecde65c`，回执 `reports/recovery-probe-20260916/verification-final/verification.json`。第二轮新增 21 项恢复测试覆盖真实 PowerShell 控制流、取消/重复运行、失败回滚和原子日志；账户/调度/权限操作仍为隔离替身。固定研究包未变化，实际管理员 Check 与专用身份运行尚未验收。

- 最终源码绑定：`b6cbd6bee9676530b28d9c480626a597c37a0de2b83819302580b8f914894d79`。
- `reports/repair-20260916/verification-final/verification.json`：1,623 项通过，0 失败/错误/跳过；lint、迁移、写者边界、已安装最小核心依赖及运行检查均通过。测试期间源码未变化；当前为未提交工作树，不是远端 CI 或现网验收。
- 10,000 条合成事件建立索引后，10 次有界最近记录/请求查询和一页历史读取共 0.118 秒；强制禁止热查询枚举事件目录，SQL 查询使用有序索引。该数值不含页面完整渲染，不能泛化为所有混合负载性能。准备合成事件和建索引耗时 63.23 秒。
- `reports/repair-20260916/stock-data-workspace-0.3.12.zip`：58 个文件，SHA-256 `294fbc9d7488be9bce86561e0df861bd9ef911a3389ec37bd9e7e29f404fbf67`。
- 隔离 0.3.11 → 0.3.12 → 0.3.11 包切换/回滚已验证，冻结指针未变。实际人类事件为 0，因此不是非空真人记录迁移验收；见 `reports/repair-20260916/rehearsal/rehearsal.json`。
- 浏览器按 `agent-browser` 技能实查日期与比较，发现并修复旧预测抬头误标问题；修复截图及范围见 `reports/repair-20260916/browser-check.md`。没有替用户保存判断。

索引运维命令（在整改目录执行；不采集、不训练）：

```powershell
# 显式检查/修复可重建缓存；不会新增业务事件
& 'D:\anaconda\python.exe' -m trade_system.v2.research_product journal-index --ensure-index
# 只读游标历史；下一页用返回的 next_before 作为 --before 参数
& 'D:\anaconda\python.exe' -m trade_system.v2.research_product journal-history --kind note
```

索引过期时应显式恢复，不删永久 guard、不覆盖原始事件。写事件后索引提交失败的重试沿用原 `request_id`，不制造重复判断。

## 剩余工作及顺序

1. **验收已有账户/任务的离线恢复路径。** 已实现 `scripts/recover_research_probe.ps1`，修复名称/SID 误判后通过 50 项专项测试；修正版管理员 Check 已通过，用户已批准一次离线探针。当前仅等待原盘中任务自然结束后的安全时段，尚未开始维护窗口或执行真实探针。它复用原 SID 和无触发器任务，成功/失败都恢复停用，不进行日更切换。具体范围、固定哈希及异常处理见 [部署修复记录](DEPLOYMENT_REPAIR_20260913.md)。旧恢复入口仍不能盲目重跑。
2. **再确认新版本和维护窗口并实施只读切换。** 0.3.10 许可已过期，不能复用或把 0.3.12 塞进旧许可。账户/任务启用、隔离 ACL、现网必要文件替换及调度接管须明确列入新批准范围。验收是专用身份成功启动、拒绝越权写入、实际日更发布及可回滚，不是仅显示 INSTALL_COMPLETE。
3. **完成旧职责退出和混合负载验收。** 历史入口与消费者逐项确认替代/停用；扩展索引到真实使用规模的关注、计划、复盘混合并发负载。不能把这些工程问题归因于缺账户资料。
4. **开展真人闭环及有效前瞻样本验证。** 用户实际保存并复盘判断；账户能力待真实脱敏快照。新增前瞻批次必须在目标开盘前冻结，逐交易日核对；QLib 继续与简单基线比较，未证明优势前不升级宣传或自动交易。

本轮没有修改原日常任务，没有启用研究账户/任务，没有清理历史数据库/失败目录，没有推送 Git。已批准暂停的月度压缩任务不在本轮变更范围内。
