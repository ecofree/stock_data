# 2025 混合单位扩样、重复证券日与导出拦截

日期：2026-09-11。隔离实现目录：`D:/accio/stock_data-retirement`。本段网络补证前按 web-access 完成前置检查；固定 14 次请求、单次调用、失败不重试，未使用其他兜底来源。没有改生产库、调度、账户、旧实验或未来样本登记。

## 结论

新取得 HiThink 和 xiaodefa 中继各 7 份日线回执，覆盖 4 只证券的 43 个证券日。对应数据库 **67 条原始记录**，单位核验全部通过，确认样本存在两套比例。但其中 **24 个证券日各有两条不同代码格式的记录**，不能把 67 条当成 67 个独立样本。

全库 2025 年只读诊断发现 **54 只证券、13,096 个重复证券日、13,096 条多余记录**，每组两条。直接按六位证券代码和日期关联存在乘法放大；本段已新增导出前重复键拦截，并在真实冻结库上验证拒绝发布、原指针不变。

这不是全市场单位修复，也不是重复记录合并完成。新批次报告保留每一条原记录，规范化字段仅供后续取舍核验，**发布的 canonical 标准记录数为 0**。

## 1. 已确认的事实与原因线索

### 两种单位口径

下表为 2025 全库有效正量/额价格记录的内部一致性诊断，不能单独作为来源单位证书：

| 库内 fetched_at 日期 | 诊断口径 | 记录数 | 新原生补证对应的原始记录数 | 抽样量/额转换系数 |
|---|---|---:|---:|---|
| 2026-07-09 | 与声明的手/千元相容 | 1,215 | 12 | 100 / 1000 |
| 2026-07-11 | 与声明的手/千元相容 | 11,881 | 12 | 100 / 1000 |
| 2026-08-11 | 量/额具有相同比例的候选 | 742,948 | 28 | 10000 / 10000 |
| 2026-08-12 | 量/额具有相同比例的候选 | 501,092 | 15 | 10000 / 10000 |

两类合计 1,257,136 条；2025 源表共 1,259,516 条，另外 2,380 条零值/无效观测保留为未认证，完整分类见分析包 `inventory_2025`。以上日期分组只是**库内记录的写入日期**，不是已认证的实际采集批次 ID，不能用于整批自动缩放。

### 重复键是另一项独立缺陷

例如 `000001` 在 2025-01-02 同时有 `000001.SZ` 和 `SZ.000001`：收盘均为 11.43，但原量分别为 1,819,596.99 和 18,195.9699。两行转换后的成交量均为 181,959,699 股；它们是同一个证券日的两份记录，不是两次独立观测。

全库的 13,096 条声明口径相容记录恰好对应 13,096 个重复证券日这一诊断结果；仍应逐行核验，不能据此执行整批删除或认定所有重复组的 OHLC/量/额相容。

### 采集来源线索，不冒充完整溯源

当前 `scripts/backfill_legacy_baostock.py` 和并行版本在写入 `tushare_daily` 时，将量和额各除以 10000，并使用 `SZ.000001` 形式的代码；插入字段未写入单位和 provider。原库相应四只证券的 BaoStock checkpoint 显示 2025 全年回填 success、243 行，完成时间与 8 月数据写入时间相邻。这与观测到的第二种比例和代码格式高度一致，是具体原因线索；未找到逐行原始 BaoStock 回执与当时运行版本绑定，**不宣称每条 8 月记录已完成真实来源认证**。

`600278` 同一全年 checkpoint 的部分记录在 8 月 11 日 23:59:59 写入，后续在 8 月 12 日 00:00:00 写入，抽样仍是同一转换比例。这直接说明跨午夜日期边界不等于新的来源或独立批次。

旧 BaoStock 入口已有未统一单位禁用提示，本段未启用、未修写原表，也未以 provider='tushare' 标签推断真实出处。

## 2. 有界补证与逐行结果

工具：`tools/v2/probe_price_unit_batches.py`。

- 证券 `002767.SZ`、`000001.SZ`、`600276.SH` 各取 2025-01-02—01-10 和 2025-12-25—12-31 两个窗口；`600278.SH` 取 2025-07-28—08-05，覆盖跨午夜写入边界。
- 原生优先；每个窗口另取中继同代码日线作交叉验证。共 14 次请求、86 个来源证券日返回，去重后为 43 个证券日。不是随机样本，也不是覆盖全年。
- 逐原始行保留两种代码格式、OHLC、量/额、原声明单位、provider、fetched_at、原行摘要、回执摘要、接收时间和转换残差。
- 明确证券交易所参数支持 SZ/SH，严格校验代码与日期，不做改码别名关联。
- 原生和中继价格/成交量一致；中继千元小数的原精度允许最多 0.50 CNY 差异。本批次对旧手/千元记录采用显式 0.50 CNY 拟合容差，原始值不改成原生值，不隐藏残差；上一段默认 0.01 CNY 容差保持不变。
- 独立从原始 HiThink 回执复算 67 条源记录：成交量最大差 0 股，成交额最大差 0.500 CNY。
- 样本中的 24 组重复记录规范化后均在约定容差内相容，但保留 `merge_authorized=false` 和 `canonical_qualified=false`。不得将报告的 row units qualified 当作导出发布资格。

当前冻结库 SHA-256：`26d7bae1461e65bb1315413c7c1f56a04048fa3e0dbbe7c56de36558e75cecfd`。数据库、原回执均未改变。

| 证据 | 路径/摘要 |
|---|---|
| 14 次原始回执 | `tmp/price-batch-receipts-20260911-r1/`，manifest `89631218cedf23985a11f5585646cb4bf3a7690ccd562ac83703cb9746c61dba` |
| 原始行规范化与重复组报告 | `tmp/price-batch-analysis-20260911-r1/result.json`，manifest `ab29c89387502f0c785e50ee078bf8be8be0d754632681740a440bad18fbba50` |
| 独立算术、checkpoint、真实导出拒绝 | `tmp/price-batch-proof-20260911-r1/result.json`，manifest `10e318182408124ad0198e5bb5e600fe70124cd9c6e6c0de01902b892fffc0e5` |
| 上一段 42 行当前代码重建 | `tmp/normalized-price-units-20260911-r2/`，manifest `c02697ecd590b70415dba7ee0c4ee7b8eb596bf67d527def4e2880d32e0776bd` |

上一段 r1 保留不覆盖。由于公共规范化函数增加显式交易所及有界精度参数，代码摘要变化，本段使用同一回执重建 r2；逐字段对比除代码摘要外完全一致，未新增或改变那 42 条记录的数值/资格。

## 3. 导出保护已落实

在 `scripts/export_qlib_features.py` 的真实导出入口、创建版本目录和计算窗口之前检查：

- 日线价格、日线基础指标、实际消费的资金、实际消费的复权因子，以及启用时的资金衍生特征。
- 检查完整计算区间，包含热身和目标尾部，而不仅是最终输出日期。消费已封存 overlay 时检查对应新关系，不因未消费旧资金表的重复而误认新关系重复。
- 任一证券/日期出现多条即报 `duplicate_research_source_key`，不 `DISTINCT`、不选最新、不取最大因子消解冲突。

真实反例：`000001` 的 2025-01-02、01-03、01-06 各两条价格，按原当日/T+1/T+2 关联结构产生 **8 行**，还没有计算基础指标等后续关联。本段实际导出在 `tushare_daily:000001:2025-01-02:2` 处被拒绝，测试用旧指针摘要不变，新版本数为 0。

这项保护可能使此前能运行的无修复层导出明确失败，这是预期结果：拒绝发布受重复影响的研究数据，不通过悄悄缩小计算区间或丢弃记录把检查变绿。已留存的旧实验/导出不自动获得当前输入资格；其是否受影响需按实际计算区间与输入指纹重新核验。

## 4. 验收与下一步

源码绑定全量回归证据：`tmp/price-batch-delivery-20260911-r1/verification.json`。**1,214 项测试通过，失败/错误/跳过均为 0**；检查期间源码不变，语法检查、迁移检查、核心最小安装依赖/入口和离线备份恢复均通过。当前源码指纹 `1c56c020aca638cc78db5410e95563cb99581ddfe10ee0035eb91b35a5c062de`。新增 16 项批次测试覆盖固定预算、合成来源拒绝、双口径重复保留、重封存篡改、原库/日期/代码改变、交易所和容差边界、五类输入关系重复拒绝、overlay 范围及旧指针保护。

沿用的 0.3.1 核心 wheel 与当前核心模块逐项一致；新增研究工具/导出脚本仍属于源码工具，并非最小安装包已包含的新功能。本段未提交/推送 Git，所有累计修改保留于隔离工作区，未部署。

下一步顺序调整为：

1. 建立**证据绑定的重复记录取舍规则与隔离标准价格层**：原始记录保留、单位/代码/价格逐字段对齐，冲突不可发布；缺少来源依据不能仅凭写入时间选择。先在已补证样本上消费验证，不整库合并。
2. 新版导出显式消费标准价格层，对未认证量/额与重复身份设置空值或阻断门槛；验证行数、价格路径、特征单位及旧指针保护。
3. 扩大独立来源/实际批次补证，处理历史身份与资金有效期关联，再适配 QLib/Alpha158 新协议和长窗口。

全市场单位资格、证券身份、原时点资格、正式研究/执行资格、真人闭环及生产切换均未完成。没有新增拟合、下单或批量清理；本段仍是隔离实现和历史补证。
