# 第 10 段：冻结历史只读适配与逐案缺字段清单 · 2026-09-10

## 本段结果

**已完成原组合输入、公告证据及父账契约之间的只读准备适配，并在实际冻结输入上运行、重算核验。没有生成父账成交/到账事件，没有恢复历史组合或登记新回测。**

本段按[第 9 段](STAGE_INTEGRATED_20260910.md)的下一步推进，不增加账户实现。原因是原输入仅含日线开收盘价和复权因子，既没有盘中观察时间/容量，也没有逐笔公司行为到账；直接塞进合成父账会混淆观察、假设与真实事件。

新增 `trade_system/v2/historical_adapter.py`、`tools/v2/run_historical_adapter.py`；配置 `docs/v2/historical_adapter_20260910.json`。只读验证原组合、审计和补证父包的身份/清单/源码绑定，原始输入按字节复制到新目录；提取日线观察、全部潜在持有窗口、公司行为候选、双时间裁决及逐案缺字段。未更改原账户、QLib、日线计算或权益逻辑。

## 实际冻结输入覆盖

原区间为 **2026-03-31 至 2026-07-29，82 个声明交易日**。输入证券身份以原始 `stock_code ↔ exchange.code` 映射为准，不用代码前缀猜交易所。

| 项目 | 实际结果 |
|---|---:|
| 原变体 | identity_hash_baseline、price_baseline、price_plus_funds，共 3 个 |
| 每个变体预测行数 | 14319，日期/证券身份完全一致，预测值各自保留 |
| 候选持有窗口 | 各 800，共 2400 |
| 所需证券日期组合 | 3606 |
| 已保留日线 | 3603 |
| 缺日线组合 | 3，未补零、未插值、未改成停牌日成交价 |
| 原缺口案件 | 11，全部与新计算的异常身份一致 |
| 公司行为候选 | 8，其中 1 件条款结构完整，不是入账资格 |
| 生成父账事件 / 新实验 | 0 / 0 |

窗口覆盖是冻结 Top-K 的潜在持有窗口，不是已经发生的实际持仓；没有因为账户早停、资料完整与否或收益好坏筛掉股票/变体。日线不包含 OHLC 中的 high/low，本段仅保留原 open/close，不补造其他价格。

## 两种时间口径分开

- **当前历史修复**：使用本次准备时已经取得的历史资料，修复截止 `2026-09-10T06:27:33.050530+00:00`。结果仍为 3 件停牌支持、7 件现金分红候选、1 件旧 schema unknown（另附转增条款候选）。不认证账户可恢复。
- **历史知识回放**：配置截止为 `2026-07-29T18:00:00+08:00`，也就是原区间末日。已导入补证的本地可用时间都晚于此截止，11 件裁决均为 unknown，不能把晚到公告当成系统当时已知。

冻结输入包在本地存在的已留痕时间为 `2026-09-10T03:59:40.872187+00:00`。这不是每条日线的历史首次接收时间。原输入没有逐行历史 availability 字段，适配后保留 `historical_row_available_at=null`、`intraday_effective_at=null`，不从日期推造收盘接收时间。能证明文件今天存在，不等于证明系统在市场当日可用这些字段。

## 逐案缺口的三个层次

可直接阅读 `reports/v2-historical-adapter-20260910/review.md`；机器清单为同目录 `report.json`。每个案件绑定所涉及的 window_id、当前修复/历史知识裁决及证据 ID。

| 案件组 | 待补来源字段 | 待声明政策 / 可推导项 |
|---|---|---|
| SZ.000609 04-22、SH.600193 04-28、SZ.300029 04-30 | 机器可消费的精确停牌区间、复牌/终止生命周期、退出所需后续原始价；来源与使用权复核 | 冻结旧价估值期限、停牌等待和退出延长区间 |
| SZ.002170 05-15、SH.605499 05-18、SZ.300534 05-22、SZ.301188 06-09、SZ.002538 06-25、SH.601666 06-30、SZ.002851 07-24 | 登记日、现金支付日、完整股份分配组成声明；来源复核 | 显式税基/税额情景、分配尾差/成本政策；登记持仓和应收批次从新父账推导 |
| SZ.301232 05-19 | 转增日期条款已有；仍缺条件性碎股分配与披露独立复核 | 税额情景、批次分配/成本政策；研究账户登记数量、到账释放事件仍需在新回放中建立 |

`structured_*` 缺口表示旧规范化证据尚未给出完整区间字段，**不意味着公告原文从未提到相关日期**。例如中迪的原公告已经回述停牌一天及复牌日期；本段不会从一段人工解释文字自动认证完整停复牌状态。

现金候选只是现金组成，不能据此把未知送股/转增组成填成 0。飞沃转增沿用显式比例 0.4，不用因子比值计算股份数。结构完整只指可通过条款 schema，不代表没有冲突、税费已知或账户到账。研究账户批次应由新的假设回放生成，不要求用户真实账户资料来冒充研究账户；真实账户验收继续独立。

## 输出、绑定和验收

`reports/v2-historical-adapter-20260910/` 包含：

- `frozen_inputs.json`：原输入 2310729 字节原样保留；预测值、完整身份映射和日历不重排覆写。
- `daily_observations.json`：3603 条日线及原输入行号；全部 `parent_market_event_ready=false`。
- `selection_windows.json`：2400 个潜在窗口及稳定身份，不是委托或成交。
- `action_candidates.json`：8 个候选的原字段、可用时间与引用，不造账户收据。
- `report.json` / `review.md`：可重算的机器/人工清单；`registration.json` / `completed.json` 绑定父包、源码及完整文件成员。

完成清单 SHA-256：`5d40dbdb2d3249093be3fca2eb36f68ca4d3263929f2e3e8b6907fb295d8bcb4`。构建前后均复核父包，来源中途变化不封包；验证入口重新加载父包并重做转换，不能仅凭重写 manifest 升格 ready。旧父账三条金标准复核通过，13 个原科学计算/证据模块与上一段摘要一致。

新增 27 项定向测试通过：全变体/窗口保留、错配/重复/标签列/非有限值、未复权字段边界、未知时间不填造、晚到证据隔离、条款/现金组成/冲突处理、异常身份与旧审计一致、新目录、来源中途变化和重新封包后的语义篡改。

Python 3.12.4 全量 **885 passed / 114.92 秒**，独立 Python 3.11.15 **885 passed / 119.27 秒**；均 0 failures/errors/skipped，各 1 条既有 DeepSeek 配置旧键 warning。原始测试报告为 `tmp/v2-adapter-source-final.xml`、`tmp/v2-adapter-py311-final.xml`。

`reports/v2-adapter-verification-20260910/verification.json` 绑定源码 SHA-256 `a8bf44fad9c27070e5f4e125c610f1b70a327a53829353e6003ea1ce0abb7986`、wheel `bed679c41ac8f8891b7c528ae6b9ac92750095526c5051515e1e0be1dbb9ba85`。离源码 `-I` 安装探针新增冻结输入适配及“不制造父账事件”核对，原父账三条金标准、13 迁移/238 表、服务/研究/证据探针继续通过。39 包兼容，Ruff、迁移静态检查和 diff 空白检查通过；未运行远端 CI。

此处的 ready=false 是实际准备结果，不是测试失败；测试通过不自动改变它。安装探针验证合成契约，实际冻结输入的运行/重算来自本段准备包，两者不相互冒充。

```powershell
D:/anaconda/python.exe tools/v2/run_historical_adapter.py verify --folder reports/v2-historical-adapter-20260910
```

新准备包使用 `build --plan <配置JSON> --output <新目录>`。必须保留源实验/特征及审计/补证父包；验证器沿原有版本、运行库和清单约束核验，缺少父包或版本变化需重新绑定，而不是跳过检查。没有数据库写入接口。

## 下一段实施顺序与验收边界

1. 从已归档公告提取并绑定机器可消费的停复牌区间；补齐 7 件现金行为的登记/支付/股份组成字段，逐项保留来源缺口与冲突，不重复无变化的中继限流尝试。
2. 明确两条路径：**日线假设诊断**须新登记估值、成交、现金/股份到账、税费和退出延长期假设；**真实盘口事件回放**须真实观察时间及容量。本父账目前仅支持合成作用域，不能通过改标签直接接入历史记录。
3. 数据与政策条件充分后，在同一账户计算实现上增加有明确作用域的历史适配，登记新诊断；旧实验不改写，无法完成的账户保留资产与 null 收益，不比较不同截断区间。

本段未联网、未写生产 DuckDB、未改调度/主复盘页、未重训、未新增订单或券商入口、未提交/推送 Git。20 个工作包/80 项任务保持原结构，项目仍在实施中。
