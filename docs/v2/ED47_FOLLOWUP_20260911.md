# ed47 审计后六项整改实施记录

2026-09-11。用户已批准按六项顺序实施。本轮在 `D:/accio/stock_data-retirement` 修改，基线 `ed47f8015295618d7445e5a08216f981a9093841`。这是实施记录，不是六项全部完成或生产放行证明。

## 1. 收盘产物与旧入口

- 根因：日页改为 compact overview 后不再内联轨迹；验收器又把首屏截断的 JS 数组当成完整概念目录。
- 日页恢复完整内联轨迹，仍仅首屏渲染 50 行；验收器优先核验完整的 `review-concept-data` JSON，保留覆盖率、日期、成员和涨停配对门禁，不把验收阈值调低。
- 两个独立信号 CLI、两个公开生成函数及收盘刷新函数，写入前必须显式提供绑定当前数据库摘要的核验副本。阶段诊断删除通知路径，不能发送旧信号。
- 历史算法测试改在真正通过 backup/restore 验证的临时副本上执行；新增未授权 CLI/API 拒写和人工表字节不变反例，不以测试开关绕过生产保护。
- 真实历史备份的 2026-09-09 复盘重建通过：390 个概念，100% 概念覆盖，290 个涨停概念、1480 对关系，首屏 50 行，约 12.67 MB。浏览器读取到 20 日内联数据，外部脚本数为 0；DOM 点击可展开到 390 行和 20 个历史日。
- 边界：这不是 9 月 10 日现网故障已恢复，更不是当前每日调度已迁移。完整内联增大了文件，仍需进一步性能优化，不能改回运行时 sidecar。

## 2. 资金与标签

- 标准化在占位行筛选之前执行；零值不再使用真假值回退；拒绝非有限数。
- 精确区分 moneyflow 与 moneyflow_ths；CNY 不重复乘一万；不把缺失大单或超大单当零补齐主力净额。
- 资金单位与成交额单位分别声明；未知单位不猜测。已有明确的 yuan/CNY 资金单位可用于研究，不能据此推定分母单位。
- 按字段保留 1/3/5/10/20 观察窗口；不足窗口为空，比例不可用不清空其他已合格字段。
- 来源、接口、映射版本和单位发生变化时切断滚动段。窗口仍是观察行口径，不能宣称完整交易日资金认证。
- 标签改为市场交易日关联；缺价保留 NULL，不沿个股下一条有效行情跳日。导出声明日历证据与标签版本，无日历时显式标注为未认证的市场观察日期。
- 独立备份副本重建约 335 万资金特征行，均保留 partial/not-certified 状态。2026 年导出 918490 行，906519 行有标签；同范围旧导出 907326 行有标签，减少 807 行。原备份与冻结导出未改写。

## 3. 运行增长

- 事件消费者在 SQL 中按上海当日下界裁剪，不再先拉取全部历史再在 Python 过滤。
- 检查点采用版本化状态增量，事件幂等查询复用账本索引，不再在每个检查点复制累计 seen 正文。保留全部账本、检查点和原始档案，旧全量检查点仍能被新代码读取。
- 512 个现金事件实测：8 段检查点合计 85151 字节，后续每段约 10598 字节，重放结果一致；恢复约 0.112 秒。此为小规模合成负载，不是生产吞吐承诺。
- **未完成：** 完整性校验和重建账户状态仍随历史增长；追加样本耗时从约 0.18 秒到 0.90 秒，测量时另有并行验收负载，不能解释为纯算法基准。尚未实现完全有界热状态，也未通过 1 万/10 万事件与复杂订单压力验收。
- 回退限制：旧 0.1.0 不认识新的增量检查点载荷。不能直接降级运行旧包读取新账本；必须准备同格式兼容且核验通过的恢复包或向前修复。

## 4. 日常闭环

- `stock-data-daily run` 新增 `--observation-db`，只读取得父候选队列的精确下一交易日价格；输入另存哈希绑定的观察文件，再传入复盘函数。
- 结果分列有价/缺价数量、人工判断状态；市场开收到收盘变化不冒充 T+1 可执行收益或操作者收益。
- `--observation-db` 数据继续标为 `legacy_table_not_native_authenticated`。不通过旧表标签伪造原生认证，不自动生成判断、账户、证书或委托。
- **未完成：** 原生价格接收凭据与日常入口贯通、真实人工判断、纸面事件/证书全队列关联和连续次日复盘实证。用户暂无真实账户资料；研究观察与工具建设不因此停止，但真实账户验收仍关闭。

## 5. 数据价值与 QLib

- 新增独立历史协议 `rolling_experiment_20260911_v2.json`；不改写 9 月 14 日开始的原前瞻登记或旧负实验。
- 去掉与 ret_1d 重复的 change_pct。四组分别为 6/10/11/15 字段；未认证比例在本次预先声明的研究范围之外，不是拟合失败后临时删列。
- 登记核对导出标签/特征版本；每折在任何拟合前验证所有组的训练字段。可选事件/结构性常量必须预先声明角色，仅依据训练集处理；默认必需字段仍拒绝全缺失和常量。
- 日频入口明确拒绝盘中时间戳，不再先截日期再丢失身份；竞价/盘中研究需另建保留时间戳的协议。
- 真实历史导出、QLib 0.9.7 / LightGBM 4.6.0 实跑：4 折 × 4 组 = 16 次拟合，抽样 29893 行，80 个样本外日 / 14302 个样本；最后 20 个观察日不评分。拟合约 38.9 秒，2 个线程，每次最多 20 轮。

| 组 | 平均日 Rank IC | Top10 平均标签（非组合收益） |
|---|---:|---:|
| 价格基线 | -0.01823 | -0.4362% |
| 加普通资金流 | -0.03252 | -0.6548% |
| 加滚动资金特征 | -0.01828 | -0.0217% |
| 合并资金组 | -0.03692 | -0.1840% |

各资金组相对基线的日均误差差异描述性区间均跨零。历史观察不足以认定可靠 alpha、独立付费来源贡献或可实现收益；这些不是随机未见的前瞻样本。不得据此上线排名、改冠军或下单。

**未完成：** 一个 HiThink 专题用途和一个 TuShare 用途的原生字段/权益/来源独立性消费验收；Alpha158 标准基线；未来样本成熟、独立对照与人工研究验收。72 因子仍是候选目录，不是完成数。

## 6. 发行与受控部署

- 核心发行版本升为 0.2.0，仍保持原显式模块闭包，没有把研究驱动或旧信号打进核心包。
- 新包在隔离最小依赖环境安装，运行安装包入口、依赖与重启核验；最终交付以本轮源码绑定的 verification.json 为准，不使用首轮测试数字代替最终版本证据。
- 2026-09-11 现场只读清点四个 StockData 任务，仍全部指向旧 `D:/accio/stock_data`：竞价返回码 1、上次收盘返回码 1、盘中正在运行（267009 是运行状态）、月度维护上次返回码 0。
- **未完成/未授权执行：** 目标主机切换、任务与目录权限隔离、动态外部消费者迁移、连续影子运行、同格式兼容回退包、最终生产批准。本轮没有停任务、改任务、删除数据或生产切换；没有推送远端 CI。

## 本轮证据路径

- `tmp/ed47-followup-measure-20260911-r2/result.json`：原始备份摘要、副本核验、资金重建、512 事件测量。
- `tmp/ed47-followup-export-20260911/features.current.json`：重新生成的 2026 年导出指针与内容清单。
- `tmp/ed47-followup-review-20260911/audit.md`、`daily_review_latest.html`：真实历史复盘生成与产物核验。
- `tmp/ed47-followup-research-20260911/historical-source-groups-20260911-v2/run/`：冻结登记、16 份模型、预测、预处理、分折指标及负结果。
- `tmp/ed47-followup-delivery-20260911-r2/verification.json`：最终源码绑定验收；须确认 local_checks_passed=true 才能引用为代码通过。
- 首轮失败证据保留：r1 测量误选了没有行情表的研究账本；首轮交付核验发现旧夹具缺 feature_version，现显式标成 unversioned_legacy，不能通过新实验的版本要求。

最终复核：1096 项测试，0 失败/错误/跳过；静态、迁移、安装依赖、核心安装入口均通过，`local_checks_passed=true`。源码摘要 `3ae7de84ec047e3f9cfc713296bb7a38f85e5934ada24fb36361553bd7965e30`，0.2.0 wheel 摘要 `f4f08e51c0d5020c9e9df4e79c17718ff6baacd5aaa0c95d1493c5ed635a6819`。验收后再次比对当前源码一致。浏览器重新加载后以键盘 Enter 展开至 20 个历史日，未出现页面错误；测试专用浏览器已关闭。原备份 SHA-256 `194fe540f6b57395e782599f0200684b14df148922838f9ce248cd4542c8faba` 在副本处理前后保持一致。

## 后续不能跳过的顺序

先完成有界热状态与复杂负载实证，再补原生价格/专题来源消费与真人闭环；独立推进 Alpha158 和未来样本研究。上述条件未齐全前，发行包只能是隔离候选，不应申请或执行整体生产切换。
