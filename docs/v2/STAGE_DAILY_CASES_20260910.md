# 第 12 段：同一父账的历史日线病例诊断 · 2026-09-10

## 本段完成的范围

**新增明确作用域的历史日线病例诊断，先登记政策再运行三个真实历史病例。东鹏在声明假设下完成现金加转增核对；中迪和创兴保留资产及 null 病例收益。不是三变体选股组合回测，不覆盖原实验，不开启真实执行。**

第 11 段只有字段候选，旧父账仅允许 synthetic。直接替换标签会混淆真实观察、历史知识和假设。本段为 `IntegratedAccount` / `EntitlementBook` 增加受类型约束的作用域、权益类型、事件名称和时间差计算入口；新增 `HistoricalAccount` / `HistoricalRights` 继承同一计算实现，不复制现金、成本、股份分配、应收与税负算法。默认 synthetic 路径、PaperBook、Service 和券商接口不变。原三条金标准的 **完整结果** 与旧文件逐项相等。

新增 `trade_system/v2/daily_account.py`、`daily_case_run.py`、`tools/v2/run_daily_cases.py` 和 `tests/test_v2_daily_cases.py`。支持登记、独立运行、离线重算；输入或源码不符停止，已登记/运行目录不覆盖。新事件明确为 `hypothetical_daily_fill`，历史账户拒绝 `synthetic_fill`。`cash_and_capitalization` 与现金加送红股语义分开，当前病例策略只接受明确红股比例为 0 的组成，不推断混合送股税基。

## 先冻结的假设（不是市场或税法事实）

配置：[daily_cases_20260910.json](daily_cases_20260910.json)。登记发生在 `2026-09-10T07:03:02.463673+00:00`，登记 ID `c58851c669afa072651833bf5b2a8374394df93b84d2f6b4574d658433b3460b`，之后才运行病例和计算损益。

| 项目 | 本次固定政策 / 边界 |
|---|---|
| 作用域 | `historical_daily_case_hypothesis_not_selection_backtest`；历史修复，不是原时点知识回放 |
| 初始资产 | 每案声明 100 股已结算、无限售股份；成本为目标日前一交易日已存原始收盘价，另有现金 100000 元；不是策略实际买入或用户真实持仓 |
| 退出 | 目标日原始收盘价或其后首个有价收盘，最多等待两个声明交易日；不从复权价、除息参考价或未来价格补价 |
| 容量 | 不受盘口容量约束的病例假设，不证明可成交性；登记事件容量上限为 1000000 股，病例持仓远低于上限 |
| 现金到账 | 仅对中登代派/指定交易 A 股情景按公告代派日假设到账，不覆盖自行派发对象，不生成真实回执 |
| 股份到账 | 如缺记账日期但明确无限售股上市日，**假设**上市日交付并释放；记录 `source_value=null`、假设值和政策名，来源字段原样保留 |
| 股份分配 | 精确到逐批整数股份；碎股不能本地凑整，直接拒绝未支持输入 |
| 现金税 | 在除权日按税前现金的固定 20% 评估、假设支付时扣缴，仅为保守计算情景，不声称适用税率或实际扣缴时点 |
| 转增税 | 本病例资本公积转增采用显式零税情景，不认证用户税额；混合红股暂不支持 |
| 交易费用 | 假设佣金 3bp、每笔最低 5 元，过户费 0.1bp、卖出税费 5bp；逐批独立退出计费，不宣称真实券商收费 |
| 停牌估值 | 明确停牌日最多沿用 1 个交易日旧价，且仅为不可交易指示估值；新鲜净资产仍为空 |
| 未知停牌终点 | 只使用已证实的闭区间或明确起始日；之后没有状态证据时为 unknown，不无限延伸停牌、不认定摘牌、不清零资产 |

事件 `at` 为本次登记修复时间；`effective_at` 为假设历史步骤日期及时间。历史观察与成交时间差使用有效时间而非伪造旧接收时间。所有事件均有 `assumption:` 来源标记，原 PDF/字段本地接收时间仍留在输入父包中。没有把结构化可用时间改成公告发布日期。

## 三个实际病例结果

实际输入来自第 11 段已归档的字段候选和第 10 段原始日线。本段没有联网补数据。每案观察区间不同，不对病例收益排序或作选股评价。

| 病例 | 区间 | 结果 | 终点保留资产 |
|---|---|---|---|
| 东鹏 SH.605499 | 05-15 至 05-20 | 假设登记 100 股，新增转增 30 股；税前现金 250 元、固定情景税额 50 元、净现金 200 元；05-18 按原始收盘价假设卖出 130 股，费用 19.42 元 | 无股份/现金应收，现金 118666.58 元；病例初始总资产 118609 元，差额 57.58 元，不是选股组合收益 |
| 中迪 SZ.000609 | 04-21 至 04-24 | 04-22 一天停牌；公告明确 04-23 复牌，但冻结输入没有复牌后的原始价，不能假造退出 | 现金 100000 元、100 股、成本 1076 元；终点净资产及完整病例收益 null |
| 创兴 SH.600193 | 04-27 至 04-30 | 04-28 开市起停牌；没有实际停牌终点，04-29/30 不推成已确认停牌 | 现金 100000 元、100 股、成本 217 元；终点净资产及完整病例收益 null |

东鹏的来源股份记账日期仍缺失；病例中的 05-18 是公开记录的假设，不是补齐了真实到账证据。假设成功不消除独立转录/使用权复核缺口。两件未完成病例没有丢弃或从报告筛除。当前 `evaluated_strategy_variants=0`，原三组各 14319 行预测与 2400 窗口全部保留在冻结输入，尚未按其买卖决策运行新历史组合。

实际包 `reports/v2-daily-cases-20260910/`：`registration.json`、完整 `inputs.json`、`run/started.json`、三个病例 JSON、`run/summary.json` / `review.md`、`run/completed.json`。每案包含初始声明、政策、假设覆盖字段、逐事件及逐日快照；成本/现金/股份残差均核对。完成清单 SHA-256 `a6c8c8a4063aa72b57c4e56189b0e61f5fa40a39a2330909875f3fb3871c799a`。

## 旧证据版本兼容

本段修改了父账/权益的类型入口，旧证据包绑定的是旧源码。**不能让旧命令忽略源码摘要，也不能替旧包重写新摘要。** 新病例登记明确绑定第 11 段保留 wheel：

`tmp/v2-disclosure-wheel-20260910/stock_data_trading_assistant-0.1.0-py3-none-any.whl`

SHA-256 `0d60c8ecd704f726261f3c9e715dc37618c73d922402ff3a517f4c1fa4f76425`。每次登记/运行/验证先检查该摘要，在有路径/大小限制的临时目录离线展开旧模块，用 `-I` 子进程运行旧版完整父包验证器；然后检查复制的整个输入与旧版验证结果相同。临时旧模块只核验旧父包，不用于新病例计算。旧包、旧 wheel 和新计算源码分别绑定，无源码门禁绕过。

升级后直接用当前源码验证第 8–11 段旧包会因来源版本变化而拒绝，这是预期保护。保留旧 wheel 及父包，使用本段 `verify` 入口递归核验即可；不是删除旧包或降级实际程序。不得清理这个旧 wheel，它现在是新病例的可重现依赖。

## 验证结果

29 项新增定向测试通过：现金加转增、延迟现金/股份到账、真实复牌缺价、已给价复牌退出、未知终点、行情/停牌矛盾、未解释因子变化、碎股拒绝、未知字段/来源冲突、历史作用域/接收时点、注册先于运行、重新封包篡改和旧 wheel 身份门禁。既有父账/权益 86 项定向测试通过，三条旧金标准完整结果相同。实际三个病例已运行并在原数据依赖环境离线重算通过。

Python 3.12.4 全量 **949 passed / 98.630 秒**，独立 Python 3.11.15 **949 passed / 96.022 秒**；均 0 failures/errors/skipped，各一条既有 DeepSeek 配置旧键 warning。XML：`tmp/v2-daily-cases-source-final.xml` / `tmp/v2-daily-cases-py311-final.xml`。Ruff、13 份迁移静态检查、diff 空白检查通过；未运行远端 CI。

`reports/v2-daily-cases-verification-20260910/verification.json` 绑定源码集合 `4f32bd317a75c6a0ef31aa682f9def120321e2fd330929e41ed1b9f4fd4170b4`、wheel `6d8ecd9259a62d18f821e40124e4180def02b8f283fbcfa2bac4c03cdca8ffbd`。离源码 `-I` 安装探针验证历史类型、130 股假设处置及未知停牌资产保留，全部旧功能探针也通过；13 迁移/238 表、39 包兼容。该探针使用构造输入，不冒充真实数据复核。

**额外真实数据安装检查发现的环境边界必须保留：** 独立 Python 3.11 核心安装环境执行真实病例 `verify_cases`，被原研究的 `registered data runtime changed` 门禁拒绝。原登记运行库是 NumPy 1.26.4、QLib 0.9.7、LightGBM 4.6.0；核心安装环境为 NumPy 2.4.6、没有 QLib/LightGBM。没有安装依赖去强行通过或绕过门禁；在匹配原依赖的 `D:/anaconda/python.exe` 中重验通过。详见同目录 `historical_runtime_boundary.json`。因此本段证明核心安装契约可运行、实际病例可在原依赖环境重算，**不证明核心安装环境可独立复核完整真实研究链**。

## 运行入口与下一段

```powershell
D:/anaconda/python.exe tools/v2/run_daily_cases.py verify --folder reports/v2-daily-cases-20260910
```

新实验依次使用 `register --plan <配置> --folder <新目录>`，再 `run --folder <同目录>`。输入数据、作用域、初始资产、费用/税额政策或代码变化都要新登记；不能在旧登记上调参运行。失败留下未封包的运行目录，不覆盖重试。

下一段优先将 **从空账户开始的三变体选股日线诊断调度器** 接到同一 `HistoricalAccount`，在运行前冻结 Top-K、票额、持有期、同日现金复用、退出等待和全区间完成规则；不沿用这三个病例的人工初始 100 股。补充多证券/多次买入/登记持仓冻结/零权益/停牌阻塞后的批次测试，给出统一时间区间的账户状态。

当前父账仍有每证券一次行为、证券数/批次数上限，必须在新调度器中显式预检；不支持时保留为未完成而非绕过。原始冻结日线只覆盖潜在持有窗口，延长退出所需日线仍可能缺失；需要给出精准追加读取清单或保持 unknown，不能由病例成功推出全组合可完成。新三变体只做历史修复假设诊断，不能声称真实成交、原时点知识回放或选股优势；QLib champion 和执行权重仍不变。

本段未联网、未写生产 DuckDB、未改调度/主复盘页、未重训或生成真实订单、未提交/推送 Git。20 个工作包/80 项任务结构保留，真实账户资料仍待办，真实执行和券商路由关闭。
