# 七条工作线：集中实施与验收边界

日期：2026-09-11。对应“七项中可以完成的，一次性自主规划完成”。仅在 `D:/accio/stock_data-retirement` 实施；不修改现网数据库、定时任务或旧冻结实验。代码、实际数据、研究、真人和部署分别报告，不折算为总完成率。

## 当前结果

| 工作线 | 已实现及实际证据 | 尚未完成的边界 |
|---|---|---|
| 1. 同群输入 | 4 只证券、117 交易日、468 行价格/每日复权/资金共同样本，SSE/SZSE 半年完整日历 | 全市场历史 PIT 股票池、生命周期和停复牌认证 |
| 2. 单位及重复治理 | 半年 702 原记录保留为 468 个标准证券日，234 组重复逐条核对；54 只全年结果见下节 | 原库不改写；未观察或冲突行不得外推认证 |
| 3. 身份与连续窗口 | 核对代码、交易所、上市/退市日期；资金万元转 CNY；61 日连续热身；缺失不填充 | 当前身份快照不等于历史档案；改码资金有效期、历史名称/停复牌和全市场资格 |
| 4. QLib/Alpha158 | 官方 158 表达式生成 228 行；90 日前缀的 120 行对照一致；468 行输入和 460 个诊断目标独立复算一致 | 正式标签 0、新拟合 0；不能宣称选股增益，旧样本外负结果不改写 |
| 5. 日常闭环 | 当日原生池 40 条、观察 20 个；昨日固定 20 候选全部取得真实次日价格；专题消费及浏览器检查 | 真人观点 0；没有真实账户/成交或真人验收 |
| 6. 负载与恢复 | 10 账户 × 3 标的、每账户最多 9 活动/未知委托，1,560 次逐笔持久化；三次重新打开全审计一致 | 十万逐笔结果见下节；不能外推通用冷盘 SLA |
| 7. 工程交付 | 新增采集、恢复、独立算术核验和反例测试；完整回归/安装/远端结果见最终记录 | 部署、影子运行、真实账户和生产切换独立验收 |

## 半年输入与 Alpha158

固定证券 `000001.SZ / 002767.SZ / 600276.SH / 600278.SH`，2025-01-01 至 06-30。55 个预列请求全部取得可解析响应。原生价格优先，中继价格交叉核对；中继分别提供每日复权、资金及两交易所日历。原生当前一年日历不用于认证 2025 全年。

价格乘每日因子、成交量（股）除因子、成交额保持 CNY；资金净额万元乘 10,000 转 CNY。`label_next_ret/label_date` 全为空；单列 `adjusted_price_target_ret` 为精确 T+1 开盘至 T+2 收盘复权价格变化，仅作诊断，不绕过生命周期、可交易性、费用和 PIT 门槛。

新协议 `native_campaign_v8_adjusted_proxy_not_training_labels` 与旧 v2/v7 隔离。Alpha158 显式使用 `adjusted_shares_CNY`，不重复乘 100/1,000；旧调用保留显式旧单位，禁止推断。四只样本不是足够的正式训练/选股验证范围。

独立核验直接读取原始响应，不调用输入推导函数；逐行复算价格、量、额、资金、诊断目标，再核对 QLib open/high/low/close/volume/vwap 二进制。完整和前缀 provider 文件均记录摘要。

| 隔离证据相对路径 | 摘要/结果 |
|---|---|
| `tmp/seven-research-receipts-20260911-r1/` | 原始清单 `242dfb8a961edcaa802d119121cb9a120f8cf35998eb9d4e822f77080a6b9d51` |
| `tmp/seven-research-input-20260911-r3/` | 当前推导清单 `7aabc7577be8c12b8a316023d76bcf931814041772f45fa22fa3384585ff0ab9` |
| `tmp/seven-alpha158-20260911-r2/` | 228 行；特征摘要 `c0194b7063c33ff1826921bcf89cc9ba20e38e76e577f8a331aaca3f382386c5` |
| `tmp/seven-independent-arithmetic-20260911-r1/result.json` | 468 行、460 目标、4 只 × 6 类底层文件一致 |
| `tmp/seven-reconciliation-20260911-r1/` | 702 原记录、468 标准日、234 重复组、0 隔离组 |

早期 r1/r2 输入是当时源码的中间证据；当前使用 r3，不重写历史文件或源码指纹。

## 全年重复键

只读副本 `tmp/ed47-followup-measure-20260911-r2/source-copy/kpl_data.duckdb`，摘要 `26d7bae1461e65bb1315413c7c1f56a04048fa3e0dbbe7c56de36558e75cecfd`。范围由实际重复键冻结，不按表现选样。

54 只全年分为 5 个无重叠窗口、9 批；每批最多 6 只/60 请求，总计 540 请求、自动重试 0。批次独立封存，恢复只重放已完成批次，原字节不覆写。首次 538 个 observed、2 个 HTTP 429；冷却后独立追加两个明确重试，各一次，均成功。原失败包保留，全部真实请求总数 542。

最终分析 `tmp/seven-duplicate-year-analysis-20260911-r2/result.json`：26,192 原版本中 26,054 条单位核验通过；13,096 重复证券日中 **13,027 组形成隔离标准价格记录、69 组继续隔离**。没有原库删除；不代表 339 万行历史行情全部修复。原采集 `tmp/seven-duplicate-year-receipts-20260911-r1/`，追加包 `tmp/seven-duplicate-year-repair-20260911-r1/`，追加清单 `4c759e82e47a11cc6d38ab31320c3a170d39743c4cc829819ad521cc6cba4cb9`。

69 个冲突集中在 38 只证券的 2025-11-27 和 12-01，均为原生与中继量/额不一致。例：000001 的 11-27 原生成交量 10,118,471 股、成交额 11,783,089 元，中继为 101,184,710 股和 1,178,308,953 元，OHLC 相同；其中 64 个案例的原生“额÷量”超出日内高低价的诊断容差。这是来源质量警报，不据此猜倍数修复、不放宽 0.50 元跨源容差。原始双源响应及所有原记录都保留，可供后续逐案核对。

## 当日观察及有界恢复

当日观察成功，但第 17 个补价请求 HTTPError；原失败工作流与前 16 份响应保留。新目录原字节保留这 16 份，额外请求 5 次（失败请求显式重试一次、3 个未请求价格、1 专题），全部成功。不能将其改称一次无失败工作流。

- 观察：`tmp/seven-daily-20260911-r1/observation/`，报告 `0812117a74129e3414852c0db33039d3d126ce1b131e005d1eee3c3b24892d99`。
- 原失败：`tmp/seven-daily-20260911-r1/native-enrichment/`。
- 恢复：`tmp/seven-daily-recovery-20260911-r1/`，20 父候选、20 价格、0 缺失、0 真人观点。
- 页面：恢复目录 `publication/runs/10654ddec1f8bc6c79e69ef09b967fe6f7f3fd8a7745a1bb1a8ecb0d05a3c641/`。

浏览器技能检查了真实生成的桌面和手机页面：20 卡片、20 选择项、20 价格，无外部脚本/资源请求、无页面横向溢出；判断锚点有效，三个文本字段必填。截图位于恢复目录 `*-browser.png / *-mobile.png`。没有代填真人观点。

恢复后补强客户端初始化失败记录和输入变化拒绝，由回归测试验证。实际恢复发生在补强前，旧结果不改写为新源码捕获；封存回执可由当前消费者重放验证。

### 0.3.2 专业复盘展示

补上此前 JSON 已有、页面未呈现的五组描述统计、覆盖/缺失、开→收/低/高区间、原始来源依据和风险、操作者原假设及失效条件。迟交标签明确，假设是否成立/失效不自动推断；空组均值显示 `—`，不是 0% 收益。

当前回执离线完整回放一致后重新生成页面，没有新增网络请求或人工观点。新发布 `tmp/seven-review-view-20260911-r1/publication/runs/a6bb85bb27fa5d20aef0de5c0eb1be375ec2404bd861783b00193d1c648c6d1e/`，旧发布保留。浏览器实际确认 5 组、20 案例、20 个依据/风险展开项，手机宽度 390 px 没有页面横向溢出，外部资源请求 0；原生展开控件支持键盘操作。新版由 frontend-design 技能约束为沿用报告式排版和信息层级，不引入外部字体、脚本或数据依赖。

该记录是页面可用性检查，不是真人研究验收；无事前观点组的描述性价格变化不能归属于任何操作者或策略。

## 复杂负载和十万持久化

`tmp/seven-multi-account-20260911-r1/result.json`：10 账户、各 3 标的及 9 活动/未知委托，1,560 次持久化合成事件。缓存最大 8 账户，热状态最大 4,584 字节；中位追加约 109 ms、P95 约 134 ms。三次重新打开全审计约 1.897 / 0.791 / 0.769 秒，状态一致。存在另一测试的磁盘竞争，不据此承诺业务吞吐。

十万原测试中断于第 35,162 条，账本/WAL/guard 保留。从持久化序号继续相同事件序列，每 1,000 条保存测量、每批最多 10,000 条，前后全重放核对。不把批量插入语料冒充逐笔提交；中断前完整延迟记录不可得，不能声称拥有十万条完整延迟分布或无中断长跑。

最终累计序号及恢复结果另补：`tmp/seven-workstreams-hot100k-20260911-r1/`、`tmp/seven-hot-resume-20260911-*/`。

## 工程交付与后续

0.3.2 当前源码绑定回归：`tmp/seven-workstreams-delivery-20260911-r3/verification.json`，**1,278 项测试，0 失败、0 错误、0 跳过**；代码/迁移检查、最小环境依赖、安装后核心入口和离线恢复均通过，检查期间源码未变。源码摘要 `8a39f9b7282f58664327c5797d2ae1fe32a71b2f3a758ba2bdc663af0d37e6bc`。此前 0.3.1 的 r1/r2 分别为 1,272/1,274 项，是中间版本记录。

安装包 `tmp/seven-wheels-20260911-r1/stock_data_trading_assistant-0.3.2-py3-none-any.whl`，108,182 字节，SHA-256 `fc94638d2d286c71a4a122b921132270f2d877b2bdbcafa8084f90dfb90ff07b`。仅升级隔离最小测试环境，旧 0.3.1 wheel 仍保留，不改变生产环境。

当前正在将完整整改代码提交到独立分支并核验远端；Git/CI 最终状态将在证据完成后补记。十万逐笔测试还在运行，不能凭本地回归通过提前销项。

研究工具从完整源码运行；核心 wheel 只包含显式发布模块，不为测试扩大核心依赖。新增工具均在 `tools/v2/`：`research_campaign.py`、`alpha158_campaign.py`、`verify_campaign_artifacts.py`、`duplicate_year_campaign.py`、`finish_daily_receipts.py`、`probe_multi_account.py`、`resume_durable_probe.py`。

正式未来窗口 2026-09-14 才开始，当前实际样本/合格共同日均 0；至少 20 个合格共同日且标签成熟之后才能讨论登记允许的研究。清单 `tmp/seven-prospective-20260911-r1/status.json`，不把零样本的零缺失计数当覆盖率通过。

下一阶段：具体来源冲突补证 → 生命周期及改码资金有效期/共同因子生产 → 真人日常试用及未来样本接收 → 成熟、合格、事前冻结的 QLib 对照 → 单独批准的部署与影子运行。旧负结果保留，不反复使用旧留置集制造胜出。

用户暂无账户文件，不重复索取或推定账户状态；真人验收、真实成交、稳定样本外增益、生产任务迁移/回滚/连续影子运行均不能由本轮测试代替。`execution_ready=false`、`production_cutover=false`。
